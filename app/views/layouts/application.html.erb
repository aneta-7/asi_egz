<!DOCTYPE html>
<html>
<head>
  <title>Wypok</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
	<!--<script>
		$(document).on('mousemove', document, function()
		{
			$("#textKom").keyup(function(e){
    	if(e.keyCode === 13)
        	$("#buttonKom").click();
    	});
		});
	</script>-->
<body>
	<header class="navbar navbar-default" role="navigation">
    <div class="navbar-inner">
      <div class="container">
        <div id="logo" class="navbar-brand"><%= link_to "Wypok", root_path %></div>
        <nav class="collapse navbar-collapse navbar-ex1-collapse">
        	<% if user_signed_in? -%>
						<ul class="nav navbar-nav navbar-right">
							<li><%= link_to 'Dodaj link', new_link_path %></li>
							<li><%= link_to 'Konto', edit_user_registration_path %></li>
							<li><%= link_to 'Wyloguj', destroy_user_session_path, :method => :delete %></li>
						</ul>
					<% else -%>
						<ul class="nav navbar-nav pull-right">
							<li><%= link_to 'Zarejestruj', new_user_registration_path %></li>
							<li><%= link_to 'Zaloguj', new_user_session_path %></li>
						</ul>
					<% end -%>
        </nav>
      </div>
    </div>
  </header>

	<div id="main_content" class="container">
		<% flash.each do |name, msg| %>
			<%= content_tag(:div, msg, class: "alert alert-info") %>
		<% end %>

			<div id="content" class="col-md-9 center-block">
				<%= yield %>
			</div>
		</div>
	</div>
</body>
</html>