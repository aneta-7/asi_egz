$('#content').append('<%= j render @links %>');
<% if @links.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @links, renderer: BootstrapPagination::Rails %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
