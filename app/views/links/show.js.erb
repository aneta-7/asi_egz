console.log('show.js.erb links');
$('#content').append('<div id="comments"><%= j render @comments %></div>');
<% if @comments.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @comments, renderer: BootstrapPagination::Rails, id: comments %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
